using UnityEngine;
using System.Collections;

public class HUDWindow : UIWindow {

	// Use this for initialization
	void Start () {

		windowID = (int)WindowConfig.windowID.HUD;
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
