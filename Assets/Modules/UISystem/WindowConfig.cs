using UnityEngine;
using System.Collections;

public class WindowConfig : Utils.SingletonMono<WindowConfig> {
	public  enum windowID
	{
		MainMenu   = 0,
		HUD        = 1,
		MessageBox = 2
	}
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
